# URL base de la API consumida por el frontend.
# En VPS con Nginx reverse proxy, usa ruta relativa:
NEXT_PUBLIC_API_BASE_URL=/api

# Alternativa sin proxy (dominio directo al backend):
# NEXT_PUBLIC_API_BASE_URL=https://api.tudominio.com/api
